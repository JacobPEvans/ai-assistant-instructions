# Validate Instruction Files - Push to main wrapper
# PRs are validated via ci-gate.yml

name: Validate Instruction Files

on:
  push:
    branches: [main]
    paths:
      - 'agentsmd/**'

jobs:
  validate-instructions:
    uses: ./.github/workflows/_validate-instructions.yml
