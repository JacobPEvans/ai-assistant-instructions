# Spell Check - Push to main wrapper
# PRs are validated via ci-gate.yml

name: Spell Check

on:
  push:
    branches: [main]
    paths:
      - '**/*.md'
      - '.cspell.json'

jobs:
  spellcheck:
    uses: ./.github/workflows/_spellcheck.yml
