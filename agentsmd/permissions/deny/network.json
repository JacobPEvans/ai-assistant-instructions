{
  "commands": [
    "curl -X POST",
    "curl -X PUT",
    "curl -X DELETE",
    "curl -X PATCH",
    "curl --request POST",
    "curl --request PUT",
    "curl --request DELETE",
    "curl --request PATCH",
    "curl -d",
    "curl --data",
    "nc -l",
    "ncat -l",
    "socat"
  ]
}
