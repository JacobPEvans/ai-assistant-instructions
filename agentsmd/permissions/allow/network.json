{
  "commands": [
    "curl -s -X GET",
    "curl -s --request GET",
    "curl --silent -X GET",
    "curl --silent --request GET",
    "curl -X GET",
    "curl --request GET",
    "ping -c",
    "nslookup",
    "dig",
    "host",
    "netstat",
    "lsof -i",
    "pgrep"
  ]
}
