# AI Assistant Instructions - Development Requirements
# These are the minimum required tools and versions for using this repository

# GitHub CLI - Required for PR management, issue operations, and workflow automation
# Minimum version: 2.0.0
# Install: https://cli.github.com/
# Verification: gh --version
gh>=2.0.0

# Git - Required for version control and worktree management
# Minimum version: 2.30.0 (for worktree support)
# Install: https://git-scm.com/downloads
# Verification: git --version
git>=2.30.0

# jq - Required for JSON parsing and manipulation in shell scripts
# Install: https://stedolan.github.io/jq/download/
# Verification: jq --version
jq>=1.6

# Optional: For development and local validation

# Pre-commit - For local hook validation (optional)
# Install: pip install pre-commit
# Verification: pre-commit --version
pre-commit>=2.20.0

# markdownlint-cli2 - For markdown linting validation (optional)
# Install: npm install -g @markdownlint/cli2
# Verification: markdownlint-cli2 --version
markdownlint-cli2>=0.11.0

# shellcheck - For shell script validation (optional, recommended)
# Install: brew install shellcheck (macOS) or apt-get install shellcheck (Linux)
# Verification: shellcheck --version
shellcheck>=0.8.0

# yamllint - For YAML validation (optional)
# Install: pip install yamllint
# Verification: yamllint --version
yamllint>=1.26.0
